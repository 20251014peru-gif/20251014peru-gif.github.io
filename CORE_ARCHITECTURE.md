# CORE_ARCHITECTURE — 독립 감각·비종속 종합추론 구조

> **핵심 원칙:** 어떤 하나의 데이터원, 프로그램, AI, API가 없어도 시스템의 기본 기능은 계속 돌아가야 한다.

이 문서는 `SYSTEM_PHILOSOPHY.md`를 실제 시스템 구조로 옮기기 위한 아키텍처 원칙이다.

---

## 1. No Single Point of Intelligence

전체 판단을 하나의 프로그램, 하나의 데이터원, 하나의 AI 모델에 종속시키지 않는다.

다음 각각은 독립 모듈이다.

- 뉴스
- 유튜브
- 공시
- 매크로
- 가격·거래량
- 수급
- 환율·금리
- 섹터·ETF 상대강도
- 기록보관실
- AI

하나가 고장나거나 데이터가 비어도 나머지 입력으로 기본 추론이 가능해야 한다.

---

## 2. AI는 필수 엔진이 아니라 선택적 기능

AI가 없어도 시스템은 다음을 수행할 수 있어야 한다.

- 공식 경제지표 저장·비교
- 공시 원문/수치 구조화
- 가격·거래량·상대강도 계산
- 금리·환율 변화 감지
- 자금흐름 후보 탐지
- 이벤트 일정 추적
- 예상치/이전값/실제값 비교
- 추세 방향·가속/둔화 판정
- 규칙 기반 신호 생성
- 근거와 반대근거 저장
- 결과 검증

AI는 다음 영역에서 보조적으로 사용한다.

- 긴 문장·공시·뉴스의 의미 추출
- 여러 자료의 문맥 연결 후보
- 가설/반대가설 후보 발굴
- 설명문·요약문 생성
- 사용자가 이해하기 쉬운 표현으로 변환

AI가 낸 내용은 **사실 데이터보다 상위 권한을 가지지 않는다.**

AI 결과가 가격·공시·공식지표 등 검증된 데이터와 충돌하면 충돌 상태를 표시하고, 사실을 덮어쓰지 않는다.

---

## 3. 데이터 신뢰 계층

정보의 역할을 구분한다.

### Tier 1 — 공식 1차 자료
- 정부·중앙은행 경제지표
- 기업 공시
- 거래소 데이터
- 공식 정책 발표
- 실적·수주·재무자료

### Tier 2 — 직접 측정 시장 데이터
- 가격
- 거래량
- 금리
- 환율
- ETF·섹터 상대강도
- 가능한 범위의 수급 데이터

### Tier 3 — 신뢰 가능한 2차 자료
- 주요 리서치
- 공신력 있는 데이터 제공사
- 검증된 보고서

### Tier 4 — 뉴스
- 사실 확산
- 새로운 정보 등장
- 시장 내러티브 변화

### Tier 5 — 유튜브·의견·해설
- 전망
- 해석
- 전문가 의견 변화
- 초기 내러티브 후보

### Tier 6 — AI 추론
- 연결·가설·설명 후보

아래 Tier라고 해서 가치가 낮다는 뜻이 아니다. **사실 확인과 해석의 역할이 다르다.**

---

## 4. 기본 판단은 데이터·규칙·통계가 담당

프로그램은 가능한 범위에서 먼저 계산 가능한 것을 계산한다.

예:

- 전월/전년 대비
- 예상치 대비 서프라이즈
- 5일/20일 상대강도
- 추세 기울기
- 가속/둔화
- 거래량 증가
- 섹터 간 성과 차이
- 금리 스프레드
- 국가 간 상대성과
- 뉴스량/새 정보량 변화

그 다음 규칙 기반으로 기본 상태를 만든다.

예:

- 성장 가속/둔화
- 물가 압력 상승/하락
- 유동성 완화/긴축
- 위험선호/위험회피
- 자금유입/유출 후보
- 이벤트 선반영 가능성

AI는 이 계산을 대체하지 않는다.

---

## 5. Graceful Degradation — 빠진 감각을 숨기지 않는다

어떤 모듈이 빠졌을 때 시스템은 멈추지 않고 **판단 범위를 축소**한다.

예:

- 뉴스 없음 → 가격·매크로·공시 기준으로 판단
- 유튜브 없음 → 초기 내러티브 선행성 판단은 약화
- 수급 없음 → 자금흐름 확신도 낮춤
- AI 없음 → 규칙 기반 기본판정만 제공
- 공시 없음 → 기업 사실 확인 수준 낮춤

출력에는 반드시 어떤 감각이 사용됐고 어떤 감각이 빠졌는지 표시한다.

---

## 6. 표준 출력 계약

각 감각 모듈은 자기 언어를 유지해도 되지만, 종합추론으로 전달할 때 최소 공통 필드를 사용한다.

초기 최소 표준:

- `topic`
- `source`
- `time`
- `observation`
- `direction`
- `evidence`
- `confidence_basis`
- `raw_source`

확장 필드:

- `event_type`
- `stage`
- `novelty`
- `related_assets`
- `counter_evidence`
- `next_check`
- `country`
- `market`
- `sector`
- `industry`

모든 필드를 모든 모듈에 강제하지 않는다. 관찰 가능한 값만 전달한다.

---

## 7. 감각 브리지(Adapter) 구조

기존 프로그램을 억지로 동일 구조로 재작성하지 않는다.

각 프로그램 옆에 작은 Adapter를 둔다.

예:

- `news_adapter`
- `youtube_adapter`
- `macro_adapter`
- `market_adapter`
- `filing_adapter`

Adapter는 기존 프로그램의 출력을 표준 신호 형식으로 번역한다.

이 방식으로 기존 프로그램의 독립성을 유지하면서 종합추론과 연결한다.

---

## 8. 종합추론의 기본 출력

종합추론은 최소 다음을 구분한다.

- 확인된 사실
- 관찰
- 가설
- 반대가설
- 현재 판정
- 미확인 사항
- 다음 확인 항목
- 사용된 감각
- 누락된 감각

근거 없는 확률 숫자를 임의로 만들지 않는다.

---

## 9. 실험·정확도 측정

새 감각을 하나 추가할 때마다 같은 사건/추세를 다시 평가한다.

비교 항목:

- 방향 적중 여부
- 선행일수
- 잘못된 신호 수
- 판단 변경 횟수
- 시장반응 예측 품질
- 설명력 개선
- 추가 감각의 실제 기여도

즉:

**감각 추가 → 동일 사건 재추론 → Before/After 비교 → 기여도 측정 → 유지/수정/제거**

결과를 알고 난 뒤 과거 판단을 수정하지 않는다. 당시 스냅샷을 보존한다.

---

## 10. 미국·한국 공통 구조, 국가별 다른 감각

미국과 한국은 같은 스키마를 사용하되 동일한 지표 구성을 강제하지 않는다.

### 미국에서 비중이 큰 축
- Fed
- CPI/PCE/PPI
- ISM
- 고용
- 2Y/10Y
- 달러
- 금융조건

### 한국에서 추가로 중요한 축
- 수출
- 반도체 수출·가격·재고
- USD/KRW
- 외국인 수급
- 중국 경기
- KOSPI/KOSDAQ 상대강도

매일 국가 간 상대매력도도 비교한다.

---

## 11. 자본흐름 계층

자금흐름은 가능한 한 다음 순서로 본다.

**자산군 → 국가 → 시장 → 스타일 → 산업 → 세부산업 → 기업**

그리고 항상:

- 빠지는 곳
- 들어가는 곳
- 현재 확산 단계
- 직접 수혜
- 2차/3차 수혜 후보
- 상대적 피해
- 자금이탈
- 다음 목적지 후보

를 구분한다.

다음 수혜 후보는 근거가 부족하면 `관찰 후보`로만 표시한다.

---

## 12. 구현 우선순위

1. 표준 신호 v0.1
2. 초소형 추론기
3. 매크로 Adapter
4. 미래 이벤트 실시간 테스트
5. 시장/자금흐름 Adapter 추가
6. 정확도 비교
7. 뉴스/유튜브/공시 순차 연결
8. 종합추론 UI 고도화

철학보다 구현이 앞서지 않는다. 그러나 철학 문서를 계속 늘리는 것 자체가 목적도 아니다. 이 문서 이후에는 **실제 데이터 계약과 테스트 규칙으로 이동**한다.

---

# 한 문장 원칙

> **AI를 포함한 모든 프로그램은 기능의 하나일 뿐이며, 어느 하나가 없어도 시스템은 작동해야 한다. 여러 독립 감각이 표준 신호로 연결되고 서로 검증될수록 종합추론의 품질이 높아지는 구조를 만든다.**
